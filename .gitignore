node_modules
dist
.DS_Store
*.log

# Rust / Tauri
src-tauri/target
src-tauri/gen/schemas

# local env
.env
.env.local

# updater signing key (keep secret, never commit)
src-tauri/updater.key

# superpowers dev specs (local planning docs, not for remote)
docs/superpowers/
